{
    "name": "twitch-digits",
    "description": "Save twitch stats to db",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "snapshot": "node ./bin/saveSnapshot",
        "dev": "cross-env NODE_ENV=development nodemon index.js"
    },
    "nodemonConfig": {
        "ignore": [
            "public_src/*"
        ],
        "delay": "500"
    },
    "dependencies": {
        "axios": "^0.21.1",
        "cors": "^2.8.4",
        "express": "^4.16.1",
        "pg": "^7.3.0"
    },
    "devDependencies": {
        "cross-env": "^6.0.3",
        "nodemon": "^2.0.2"
    }
}
